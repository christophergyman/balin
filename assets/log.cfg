# Balin log thresholds. Reloads on save while the game runs.
# Levels: trace, debug, info, warn, error, fatal.
# Keys: default plus boot, engine, input, render, audio, assets, game, ai, combat, editor.

default = info
engine = debug
